# Production: set the deployed FastAPI base URL. The build accepts either
# https://api.example.com or https://api.example.com/api/v1 and normalizes it.
VITE_API_URL=

# Local development only.
VITE_BACKEND_URL=http://127.0.0.1:8889
VITE_API_TIMEOUT=120000
